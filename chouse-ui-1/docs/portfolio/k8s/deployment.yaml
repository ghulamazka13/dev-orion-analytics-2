apiVersion: apps/v1
kind: Deployment
metadata:
  name: chouseui
  namespace: clickhouse
  labels:
    app: chouseui
spec:
  replicas: 1
  selector:
    matchLabels:
      app: chouseui
  template:
    metadata:
      labels:
        app: chouseui
    spec:
      imagePullSecrets:
      - name: ghcr-secret
      containers:
      - name: chouseui
        image: ghcr.io/daun-gatal/chouse-ui-portfolio:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 3000
          name: http
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "100m"
        livenessProbe:
          httpGet:
            path: /
            port: 3000
          initialDelaySeconds: 10
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /
            port: 3000
          initialDelaySeconds: 5
          periodSeconds: 10

---
apiVersion: v1
kind: Service
metadata:
  name: chouseui
  namespace: clickhouse
  labels:
    app: chouseui
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: chouseui

---
kind: Ingress
apiVersion: networking.k8s.io/v1
metadata:
  name: chouseui-funnel
  namespace: clickhouse
  annotations:
    tailscale.com/funnel: 'true'
spec:
  ingressClassName: tailscale
  tls:
    - hosts:
        - chouseui
  rules:
    - host: chouseui
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: chouseui
                port:
                  number: 80