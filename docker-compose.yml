version: "3.8"

services:
  postgres:
    image: postgres:16.3
    ports:
      - "15432:5432"
    environment:
      - POSTGRES_USER=airflow
      - POSTGRES_PASSWORD=airflow
      - POSTGRES_DB=airflow
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./postgres/init:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U airflow -d airflow"]
      interval: 10s
      timeout: 5s
      retries: 10

  clickhouse:
    image: clickhouse/clickhouse-server:24.3
    ports:
      - "18123:8123"
      - "19000:9000"
    environment:
      - CLICKHOUSE_USER=admin
      - CLICKHOUSE_PASSWORD=admin
      - CLICKHOUSE_DEFAULT_ACCESS_MANAGEMENT=1
      - KAFKA_BROKER_LIST=${KAFKA_BROKER_LIST:-10.110.12.20:9092}
      - KAFKA_TOPIC_LIST=${KAFKA_TOPIC_LIST:-malcolm-logs}
      - KAFKA_GROUP_SUFFIX=${HOSTNAME}
    volumes:
      - clickhouse_data:/var/lib/clickhouse
      - ./clickhouse/init:/docker-entrypoint-initdb.d:ro
    restart: unless-stopped

  chouse:
    build:
      context: ./chouse-ui
      dockerfile: Dockerfile
    ports:
      - "18087:5521"
    environment:
      - NODE_ENV=production
      - PORT=5521
      - STATIC_PATH=/app/dist
      - RBAC_DB_TYPE=sqlite
      - SESSION_TTL=28800000
      - RBAC_SQLITE_PATH=/app/data/rbac.db
      - JWT_SECRET=dev-jwt-secret-change-me-please-32chars
      - RBAC_ENCRYPTION_KEY=dev-encryption-key-change-me-please-64chars-0000000000000000
      - RBAC_ENCRYPTION_SALT=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef
    volumes:
      - chouse_data:/app/data
    depends_on:
      - clickhouse
    restart: unless-stopped

  airflow-init:
    image: apache/airflow:2.9.2-python3.11
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      - AIRFLOW__CORE__EXECUTOR=LocalExecutor
      - AIRFLOW__DATABASE__SQL_ALCHEMY_CONN=postgresql+psycopg2://airflow:airflow@postgres:5432/airflow
      - AIRFLOW__CORE__FERNET_KEY=H8oF2K4U7b8C3jY2nG9YF0pI5J6kT8mP1qR2sX3u4vE=
      - AIRFLOW__WEBSERVER__SECRET_KEY=airflowsecret
      - AIRFLOW__CORE__LOAD_EXAMPLES=False
      - AIRFLOW__CORE__DAGS_ARE_PAUSED_AT_CREATION=False
      - _AIRFLOW_WWW_USER_USERNAME=admin
      - _AIRFLOW_WWW_USER_PASSWORD=admin
      - _AIRFLOW_WWW_USER_FIRSTNAME=Admin
      - _AIRFLOW_WWW_USER_LASTNAME=User
      - _AIRFLOW_WWW_USER_EMAIL=admin@example.com
      - CLICKHOUSE_HOST=clickhouse
      - CLICKHOUSE_PORT=8123
      - CLICKHOUSE_USER=etl_runner
      - CLICKHOUSE_PASSWORD=etl_runner
      - CLICKHOUSE_DATABASE=default
      - _PIP_ADDITIONAL_REQUIREMENTS=clickhouse-connect==0.7.16 pyyaml==6.0.2
    volumes:
      - ./airflow/dags:/opt/airflow/dags
    entrypoint: /bin/bash
    command:
      - -c
      - |
        airflow db migrate
        airflow users create \
          --role Admin \
          --username "$$_AIRFLOW_WWW_USER_USERNAME" \
          --password "$$_AIRFLOW_WWW_USER_PASSWORD" \
          --firstname "$$_AIRFLOW_WWW_USER_FIRSTNAME" \
          --lastname "$$_AIRFLOW_WWW_USER_LASTNAME" \
          --email "$$_AIRFLOW_WWW_USER_EMAIL"
    restart: "no"

  airflow-webserver:
    image: apache/airflow:2.9.2-python3.11
    depends_on:
      airflow-init:
        condition: service_completed_successfully
    ports:
      - "18088:8080"
    environment:
      - AIRFLOW__CORE__EXECUTOR=LocalExecutor
      - AIRFLOW__DATABASE__SQL_ALCHEMY_CONN=postgresql+psycopg2://airflow:airflow@postgres:5432/airflow
      - AIRFLOW__CORE__FERNET_KEY=H8oF2K4U7b8C3jY2nG9YF0pI5J6kT8mP1qR2sX3u4vE=
      - AIRFLOW__WEBSERVER__SECRET_KEY=airflowsecret
      - AIRFLOW__CORE__LOAD_EXAMPLES=False
      - AIRFLOW__CORE__DAGS_ARE_PAUSED_AT_CREATION=False
      - CLICKHOUSE_HOST=clickhouse
      - CLICKHOUSE_PORT=8123
      - CLICKHOUSE_USER=etl_runner
      - CLICKHOUSE_PASSWORD=etl_runner
      - CLICKHOUSE_DATABASE=default
      - _PIP_ADDITIONAL_REQUIREMENTS=clickhouse-connect==0.7.16 pyyaml==6.0.2
    volumes:
      - ./airflow/dags:/opt/airflow/dags
    command: webserver

  airflow-scheduler:
    image: apache/airflow:2.9.2-python3.11
    depends_on:
      airflow-init:
        condition: service_completed_successfully
    environment:
      - AIRFLOW__CORE__EXECUTOR=LocalExecutor
      - AIRFLOW__DATABASE__SQL_ALCHEMY_CONN=postgresql+psycopg2://airflow:airflow@postgres:5432/airflow
      - AIRFLOW__CORE__FERNET_KEY=H8oF2K4U7b8C3jY2nG9YF0pI5J6kT8mP1qR2sX3u4vE=
      - AIRFLOW__WEBSERVER__SECRET_KEY=airflowsecret
      - AIRFLOW__CORE__LOAD_EXAMPLES=False
      - AIRFLOW__CORE__DAGS_ARE_PAUSED_AT_CREATION=False
      - CLICKHOUSE_HOST=clickhouse
      - CLICKHOUSE_PORT=8123
      - CLICKHOUSE_USER=etl_runner
      - CLICKHOUSE_PASSWORD=etl_runner
      - CLICKHOUSE_DATABASE=default
      - _PIP_ADDITIONAL_REQUIREMENTS=clickhouse-connect==0.7.16 pyyaml==6.0.2
    volumes:
      - ./airflow/dags:/opt/airflow/dags
    command: scheduler

  superset:
    build: ./superset
    ports:
      - "18089:8088"
    environment:
      - SUPERSET_SECRET_KEY=supersetsecret
      - SUPERSET_LOAD_EXAMPLES=no
      - SUPERSET_ENV=production
      - SUPERSET_SQLALCHEMY_DATABASE_URI=sqlite:////app/superset_home/superset.db
      - SQLALCHEMY_DATABASE_URI=sqlite:////app/superset_home/superset.db
    volumes:
      - superset_data:/app/superset_home
      - ./superset/superset_config.py:/app/pythonpath/superset_config.py:ro
    depends_on:
      superset-init:
        condition: service_completed_successfully

  superset-init:
    build: ./superset
    user: "0"
    environment:
      - SUPERSET_SECRET_KEY=supersetsecret
      - SUPERSET_LOAD_EXAMPLES=no
      - SUPERSET_ENV=production
      - SUPERSET_SQLALCHEMY_DATABASE_URI=sqlite:////app/superset_home/superset.db
      - SQLALCHEMY_DATABASE_URI=sqlite:////app/superset_home/superset.db
    volumes:
      - superset_data:/app/superset_home
      - ./superset/superset_config.py:/app/pythonpath/superset_config.py:ro
    entrypoint: /bin/bash
    command:
      - -c
      - |
        chown -R superset:superset /app/superset_home
        superset db upgrade
        superset fab create-admin \
          --username admin \
          --firstname Admin \
          --lastname User \
          --email admin@example.com \
          --password admin
        superset db upgrade
        superset init
    restart: "no"

  opensearch-puller:
    build:
      context: ./services/opensearch_puller
      dockerfile: Dockerfile
    environment:
      - POSTGRES_DSN=postgresql://airflow:airflow@postgres:5432/airflow
      - CLICKHOUSE_HTTP_URL=http://admin:admin@clickhouse:8123
      - BATCH_SIZE=500
      - OVERLAP_MINUTES=10
      - POLL_INTERVAL_SECONDS=30
      - LOG_LEVEL=INFO
      - OPENSEARCH_VERIFY_SSL=true
      - ITSEC_SECRET_KEY=${ITSEC_SECRET_KEY:-}
      - ITSEC_UI_PASSWORD=${ITSEC_UI_PASSWORD:-admin123!}
    volumes:
      - ./secrets:/run/secrets:ro
    depends_on:
      - postgres
      - clickhouse
    restart: unless-stopped

  itsec-datapipeline-manager:
    build:
      context: .
      dockerfile: services/datapipeline_manager/Dockerfile
    ports:
      - "18090:8081"
    environment:
      - POSTGRES_DSN=postgresql://airflow:airflow@postgres:5432/airflow
      - CLICKHOUSE_HTTP_URL=http://admin:admin@clickhouse:8123
      - OPENSEARCH_VERIFY_SSL=true
      - ITSEC_UI_USER=admin
      - ITSEC_UI_PASSWORD=admin123!
      - ITSEC_SECRET_KEY=${ITSEC_SECRET_KEY:-}
      - ITSEC_PAGE_SIZE=20
    volumes:
      - ./secrets:/run/secrets:ro
    depends_on:
      - postgres
      - clickhouse
    restart: unless-stopped

  schema-migrator:
    build:
      context: ./services/schema_migrator
      dockerfile: Dockerfile
    environment:
      - POSTGRES_DSN=postgresql://airflow:airflow@postgres:5432/airflow
      - CLICKHOUSE_HTTP_URL=http://admin:admin@clickhouse:8123
      - LOG_LEVEL=INFO
    depends_on:
      - postgres
      - clickhouse
    command: ["apply"]
    restart: "no"

volumes:
  postgres_data:
  clickhouse_data:
  chouse_data:
  superset_data:
